// The renderer and Goose binary come from this repository revision, so their
// generated extension methods must stay in lockstep. The published package at
// the same nominal version can lag behind the workspace-generated client.
export * from "../../../harness bone/ui/sdk/src/index.ts";
